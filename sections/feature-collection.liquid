<div class="title-container">
    <h1 class="title">{{ section.settings.title }}</h1>
</div>

{% for products in section.settings.feature_collection.products %}
    {% render "products", product: products %}
{% else %}
    {% for item in section.settings.feature_collection.products limit: 4 %}
        {{ 'product-1' |  placeholder_svg_tag }}
    {% endfor %}
{% endfor %}

{% schema %}
{
    "name": "template collection part2",
    "settings": [
        {
            "type": "text",
            "id": "title",
            "default": "feature collection",
            "label": "title"
        },
        {
            "type": "collection",
            "id": "feature_collection",
            "label": "Collection"
        }    
    ]
}
{% endschema %}
